[project]
name = "scoring_api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mypy>=1.15.0",
    "pytest>=8.3.5",
    "pytest-mock-resources[redis]>=2.12.2",
    "redis>=5.2.1",
    "ruff>=0.9.10",
]
